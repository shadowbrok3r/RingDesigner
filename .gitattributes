# Text the app carries with include_str! and its tests compare, and the prose beside it: LF on every checkout.
*.md text eol=lf
/LICENSE-* text eol=lf
# The vendored crates' notes keep the bytes they came with.
patches/**/*.md -text
