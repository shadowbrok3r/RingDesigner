//! What a right-click on the band offers for pins: reference points snaps and Measure read from.
use super::menu::MenuItem;

/// Pin items for a point of the band.
pub fn band_items(_world: [f64; 3]) -> Vec<MenuItem> {
    Vec::new()
}
